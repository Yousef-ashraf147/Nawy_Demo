import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "newcairo-developments.com",
      "selecthouse.co",
      "images.bayut.eg",
      "localhost",
      "127.0.0.1",
    ],
  },
};

export default nextConfig;
